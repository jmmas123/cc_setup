Quick project orientation. Do all of the following:

1. **State file**: Look for `docs/STATE.md`, `STATE.md`, or `.claude/STATE.md`. If found, read it and summarize the current position, recent decisions, and any blockers. If not found, note that no state file exists.

2. **Git status**: Show the current branch, any uncommitted changes (staged and unstaged), and the last 5 commit messages with short hashes.

3. **Project structure**: Check if `CLAUDE.md` exists at the project root. Check if `docs/ROADMAP.md` exists. Briefly note what project documentation is available.

4. **Summary**: Provide a concise orientation paragraph: what project this is, where we are, and what's pending.

Keep the output focused and scannable. Use headers and bullet points.
