# DWH MCP Server credentials
# Copy to .env and fill in your values
DB_DRIVER=postgresql          # "postgresql" or "sqlserver"
DB_HOST=localhost
DB_PORT=9700                  # PostgreSQL: 9700, SQL Server: 1433
DB_NAME=datax
DB_USER=your_username
DB_PASSWORD=your_password     # leave empty if no password
